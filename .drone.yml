kind: pipeline
type: docker
name: default

steps:
- name: test
  image: python:3.12.0a3-alpine3.17
  environment:
    TESTY: test-environment-variable
  commands:
  - echo hello
  - echo world
  - echo "$PWD"
  - echo "$HOSTNAME"
  - echo $(ls)
  - python python-file.py

trigger:
  branch:
  - main
  event:
  - push